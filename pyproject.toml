[project]
name = "cryptosafe-manager"
version = "0.1.0"
description = "Production-grade Sprint 1 foundation"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-ra -q"

[tool.coverage.run]
source = ["src"]
